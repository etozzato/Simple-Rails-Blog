<h3><%= h(post.title) %></h3>

<div id="post-body">
  <%= add_breaks(auto_link(h(post.body), :all, :target => "_blank")) %>
</div>

<div id="post-data">
  published on <%= h_date(post.published_at) %>
</div>